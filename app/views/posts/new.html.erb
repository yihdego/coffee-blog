<%= form_for @post, html: {class: "new-post"} do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.text_area :body, size: "60x12", placeholder: "Describe the blend " %>
  <%= f.text_field :location, {placeholder: "Location "}%>
  <%= f.submit "Create" %>
<% end %>
